제공하신 노트북 파일을 기반으로 **PyTorch만 사용하는 것**과 **WandB를 추가하는 것**의 차이를 구체적으로 설명드리겠습니다.

---

## 📊 **핵심 차이점 분석**

### **1. 학습 과정 추적 (Tracking)**

#### **PyTorch만 사용할 때:**
```python
def train_loop(dataloader, model, loss_fn, optimizer):
    # ...
    print(f"Train Error: \n Accuracy: {(100*train_acc):>0.1f}%, Avg loss: {train_loss:>8f} \n")
```
- `print()` 문으로 콘솔에만 출력
- **문제점**: 
  - Colab 세션이 끊기면 모든 기록 손실
  - 실험 비교가 어려움
  - 나중에 어떤 하이퍼파라미터를 사용했는지 기억하기 어려움

#### **WandB를 추가할 때:**
```python
wandb.init(
    project="i-hate-tomatoes",
    name="trial_0",
    config={
        "learning_rate": learning_rate,
        "batch_size": batch_size,
        "epochs": epochs,
        "hidden_dim": hidden_dim,
    })

wandb.log({
    "train_acc": train_acc,
    "train_loss": train_loss,
    "validation_acc": validation_acc,
    "validation_loss": validation_loss,
})
```
- **장점**:
  - 모든 지표가 **클라우드에 자동 저장**
  - 하이퍼파라미터가 자동으로 기록됨
  - 세션이 끊겨도 데이터 유지

---

### **2. 시각화 (Visualization)**

#### **PyTorch만 사용:**
```python
# 직접 matplotlib으로 그려야 함
import matplotlib.pyplot as plt

epochs_list = [1, 2, 3, 4, 5]
train_losses = [2.247, 2.109, 1.882, 1.590, 1.339]

plt.plot(epochs_list, train_losses)
plt.xlabel('Epoch')
plt.ylabel('Loss')
plt.show()
```
- **문제점**: 
  - 매번 코드를 작성해야 함
  - 여러 실험을 비교하려면 데이터를 수동으로 저장하고 그려야 함

#### **WandB 사용:**
```python
# 자동으로 실시간 대시보드 생성
wandb.log({"train_loss": train_loss, "validation_loss": validation_loss})
```
- **장점**:
  - 자동으로 **인터랙티브 차트** 생성
  - Smoothing, 범위 조정 등 **웹 UI에서 간편하게 조작**
  - 여러 실험을 한 화면에서 비교 가능

---

### **3. 이미지 로깅 (Image Logging)**

이 노트북의 코드를 보면:

```python
sample_img = to_pil_image(sample_batch[0])
sample_img = wandb.Image(sample_img, caption="sample image")

images = [to_pil_image(image) for image in sample_batch[:5]]

wandb.log({
    "example": sample_img,
    "examples": [wandb.Image(image) for image in images],
})
```

- **단일 이미지**와 **여러 이미지**를 WandB에 로깅
- Fashion MNIST 데이터셋의 샘플 이미지들을 epoch마다 자동으로 기록
- **실제 학습 데이터를 시각적으로 확인** 가능

#### **PyTorch만 사용할 때:**
- 이미지를 저장하려면 별도로 파일로 저장하거나 matplotlib로 그려야 함
- 여러 epoch의 이미지를 관리하기 어려움

---

### **4. 실험 비교 (Experiment Comparison)**

#### **코드의 하이퍼파라미터:**
```python
learning_rate = 1e-3
batch_size = 64
epochs = 5
hidden_dim = 512
```

만약 이 값들을 바꿔가며 여러 실험을 한다면:

#### **PyTorch만 사용:**
- 수동으로 Excel이나 노트에 기록
- 어떤 설정이 가장 좋았는지 찾기 어려움

#### **WandB 사용:**
```python
# 실험 1
wandb.init(name="trial_0", config={"learning_rate": 1e-3, "hidden_dim": 512})

# 실험 2
wandb.init(name="trial_1", config={"learning_rate": 1e-4, "hidden_dim": 1024})
```
- WandB 대시보드에서 **모든 실험을 테이블로 정리**
- 어떤 하이퍼파라미터 조합이 최고 성능을 냈는지 **자동으로 정렬 및 필터링**

---

## 📈 **이 노트북의 실행 결과**

노트북 실행 후 WandB에서 볼 수 있는 것들:

1. **차트 (Charts)**:
   - `train_acc`, `train_loss` → 학습 정확도/손실 그래프
   - `validation_acc`, `validation_loss` → 검증 정확도/손실 그래프
   - **실시간으로 업데이트**되며, smoothing 적용 가능

2. **이미지 (Images)**:
   - `example`: 단일 Fashion MNIST 이미지
   - `examples`: 5개의 샘플 이미지들
   - Epoch마다 어떤 데이터가 학습되었는지 시각적으로 확인

3. **하이퍼파라미터 자동 기록**:
   - learning_rate: 0.001
   - batch_size: 64
   - epochs: 5
   - hidden_dim: 512

---

## 🎯 **실제 사용 시나리오**

### **PyTorch만 사용 (기본 방식):**
```
실험 1 실행 → 노트북에 출력 → 결과 복사해서 메모장에 기록
실험 2 실행 → 노트북에 출력 → 또 메모장에 기록
...
나중에 "어? 실험 1의 learning rate가 뭐였지?" → 찾기 어려움
```

### **WandB 사용 (이 노트북 방식):**
```
실험 1 실행 → WandB에 자동 저장
실험 2 실행 → WandB에 자동 저장
...
웹 브라우저에서 https://wandb.ai 접속 → 모든 실험 한눈에 비교
```

---

## 💡 **요약**

| 기능 | PyTorch만 | PyTorch + WandB |
|------|----------|----------------|
| **기록 방식** | 콘솔 출력 (`print`) | 클라우드 저장 |
| **데이터 보존** | 세션 끊기면 손실 | 영구 보존 |
| **시각화** | 수동 코딩 필요 | 자동 생성 |
| **실험 비교** | 매우 어려움 | 웹 UI에서 간편 |
| **이미지 로깅** | 수동 저장 | 자동 로깅 |
| **협업** | 어려움 | 링크 공유로 간편 |

---

이 노트북의 핵심은 **"실험 관리의 자동화"**입니다. WandB를 추가함으로써 데이터 과학자는 코드 작성보다는 **모델 개선에 집중**할 수 있게 됩니다! 🚀
